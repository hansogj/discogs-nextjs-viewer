services:
  # Redis cache and queue service
  cache:
    image: redis:7.4-alpine
    restart: always
    ports:
      - "6379:6379"
    env_file:
      - ../.env.local
    # AOF (fsync every second) + RDB snapshot (every 20s if >=1 change)
    # give us belt-and-braces durability for cached user data, which now
    # lives exclusively in Redis after the file cache was retired.
    command: sh -c "redis-server --appendonly yes --appendfsync everysec --save 20 1 --loglevel warning --requirepass $$REDIS_PASSWORD"
    volumes:
      - cache:/data
    healthcheck:
      test: ["CMD-SHELL", "redis-cli -a $$REDIS_PASSWORD ping"]
      interval: 10s
      timeout: 5s
      retries: 5

  # Next.js web application
  web:
    build:
      context: .. # Dockerfile is in the parent directory
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    env_file:
      - ../.env.local
    environment:
      NODE_ENV: development
    command: pnpm dev
    depends_on:
      cache:
        condition: service_healthy # Wait for Redis to be healthy

  # BullMQ worker for background tasks
  worker:
    build:
      context: .. # Dockerfile is in the parent directory
      dockerfile: Dockerfile
    env_file:
      - ../.env.local
    environment:
      NODE_ENV: development
    command: pnpm dev:worker
    depends_on:
      cache:
        condition: service_healthy # Wait for Redis to be healthy

volumes:
  cache:
    driver: local
