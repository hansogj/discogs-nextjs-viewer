# Retained ignores for advisories in dev-only tooling that cannot be cleared
# without a major upstream change (see per-entry reason). Every entry has an
# ignoreUntil date to force periodic reassessment.
#
# No active ignores at the moment — GHSA-mh99-v99m-4gvg (brace-expansion 1.x)
# was pruned once the scanner reported it unused.
